---
import BaseLayout from "./BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

const fm = Astro.props.frontmatter;
const date = new Intl.DateTimeFormat("zh-CN", { year: "numeric", month: "long", day: "numeric" }).format(new Date(fm.date));
---

<BaseLayout title={`${fm.title} · Ury 的知识花园`} description={fm.description}>
  <Header />
  <main class="site-shell article-shell">
    <article>
      <header class="article-header">
        <h1>{fm.title}</h1>
        <div class="article-meta">
          <span>{date}</span>
          <span>{fm.type}</span>
          <span>{fm.topic}</span>
          <span>{fm.readingMinutes || 1} 分钟阅读</span>
        </div>
      </header>
      <div class="article-content">
        <slot />
      </div>
    </article>
    <aside class="panel article-side">
      <div class="panel-title">
        <strong>来源</strong>
      </div>
      <p class="section-kicker">这是一份从 Obsidian 生成的公开副本，源库不会被改写。</p>
      <p class="small-caps">{fm.sourceFolder}</p>
      <p><a class="button" href="/archive/">返回归档</a></p>
    </aside>
  </main>
  <Footer />
</BaseLayout>
