---
layout: ../../../../../layouts/PostLayout.astro
title: 多表查询
description: 当然，下面为你详细讲解这条SQL查询语句与注释之间的联系，以及代码是如何一步步写出来的。 1. 注释内容分析 注释：
  查询所有价格在10元 含 到50元 含 之间且状态为'起售'的菜品，展示出菜品的名称、价格、分类名称。 要点分解： 范围：
date: 2025-07-14T03:56:00.000Z
updated: 2025-07-14T03:56:00.000Z
type: 思考
topic: 编程错题集
tags: []
readingMinutes: 3
sourceFolder: 计算机编程/编程错题集/数据库报错
---

当然，下面为你详细讲解这条SQL查询语句与注释之间的联系，以及代码是如何一步步写出来的。

---

## 1. 注释内容分析

**注释：**

> 查询所有价格在10元(含)到50元(含)之间且状态为'起售'的菜品，展示出菜品的名称、价格、分类名称。

**要点分解：**

- 范围：价格在10元到50元之间（含边界）
    
- 状态：菜品状态为“起售”
    
- 展示字段：菜品名称、价格、分类名称
    
- 涉及表：菜品表（dish），分类表（category）
    

---

## 2. SQL语句结构分析

sql

`SELECT *  FROM db_04.dish d  LEFT JOIN db_04.category c ON d.category_id = c.id  WHERE d.price BETWEEN 10 AND 50    AND d.status = 1;`

**逐步对应注释：**

## a. 表的选择与连接

- `FROM db_04.dish d`
    
    - 从菜品表（dish）查询数据，`d`是dish表的别名。
        
- `LEFT JOIN db_04.category c ON d.category_id = c.id`
    
    - 用LEFT JOIN连接分类表（category），`c`是category表的别名。
        
    - 连接条件是菜品表的`category_id`等于分类表的`id`，这样可以查出每道菜对应的分类名称。
        

## b. 条件筛选

- `WHERE d.price BETWEEN 10 AND 50`
    
    - 筛选价格在10元到50元之间的菜品，包含10和50。
        
- `AND d.status = 1`
    
    - 只查状态为1的菜品（通常1代表“起售”，具体要看你的表结构定义）。
        

## c. 字段选择

- `SELECT *`
    
    - 查询所有字段。**但根据注释，只需菜品名称、价格、分类名称**，实际可优化为：
        
        sql
        
        `SELECT d.name, d.price, c.name AS category_name FROM db_04.dish d LEFT JOIN db_04.category c ON d.category_id = c.id WHERE d.price BETWEEN 10 AND 50   AND d.status = 1;`
        
    - 这样结果更直观，也更符合注释的需求。
        

---

## 3. 代码是怎么写出来的？

1. **明确需求**：根据注释，确定要查哪些表、哪些字段、什么条件。
    
2. **确定表关系**：菜品和分类之间通过`category_id`关联，需用JOIN。
    
3. **写SELECT语句**：选择需要展示的字段。
    
4. **加WHERE条件**：根据价格和状态筛选数据。
    
5. **优化字段**：只查需要的字段，避免SELECT *。
    

---

## 4. 总结

- 注释描述了业务需求，SQL语句则是对需求的技术实现。
    
- 每一步SQL的写法都直接对应注释中的描述。
    
- 优化建议：将SELECT * 改为 SELECT d.name, d.price, c.name AS category_name，使结果更符合注释意图。
    

---

如需进一步优化或有其他需求，欢迎继续提问！
